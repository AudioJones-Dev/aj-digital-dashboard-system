# aj-digital-dashboard-system — environment
# Hub runs on this port (npm/pnpm dev → next dev -p 7500)
HUB_PORT=7500

# No secrets required for the local-first hub. It reads each project's
# /api/data and /api/manifest over localhost. Auth + deploy config come later.
